url: https://janickweberpals.gitlab-pages.partners.org/smdi/index
template:
  bootstrap: 5

# Our GitLab runner can just deploy public/
destination: public/

navbar:
  structure:
    left: [intro, reference, articles, tutorials, news, reports]
    right: [search]
  components:
   reports:
    text: Reports
    menu:
    - text: Coverage report
      href: coverage.html # covr::gitlab()
